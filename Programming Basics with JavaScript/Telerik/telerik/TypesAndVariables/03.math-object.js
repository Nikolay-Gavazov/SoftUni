let pi = Math.PI; // the number PI = 3.1415...

let roundPi = Math.round(pi); // round a number according to math rules

let piSquared = Math.pow(Math.PI, 2); // power a number

let maxOfThree = Math.max(2, -7, 4.5); // max of a few numbers

console.log(pi);
console.log(roundPi);
console.log(piSquared);
console.log(maxOfThree);
