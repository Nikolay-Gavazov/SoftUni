// the floating point numbers sometimes could not be represented in the computer memory exactly
// so there is a strange behavior at first glance 
// each number is represented as power of 2
let a = 1.33;
console.log(a.toFixed(20)); 
